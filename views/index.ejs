<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Management Web App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<div>
    <div>
        <h1 class="mt-4 text-2xl font-bold text-center text-red-600">File Management Web App</h1>
        <h2 class="mt-2 text-lg text-gray-700 text-center">Created by Adarsh Fulzele</h2>
    </div>

    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form  p-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full rounded-md outline-none  px-5 py-2 bg-zinc-800" type="text" name="title"
                    placeholder="Tile goes here" id="">
                <textarea class="block w-full rounded-md outline-none  mt-2 px-5 py-2 bg-zinc-800 resize-none"
                    name="details" placeholder="Write Your task details here" id=""></textarea>
                <input class="mt-2 bg-blue-700 px-5 py-2 rounded-md" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-white text-3xl tracking-tiighter">
                            <%=val%>
                        </h1>
                        <div class="flex w-full justify-between mt-3">
                            <a class="text-blue-500 inline-block" href="/files/<%= val%>">Read More</a>
                            <a class="text-zinc-300" href="/edit/<%= val %>">edit file name</a>
                        </div>
                    </div>
                    <% }) %>
                        <% } else{ %>
                            <h3 class="text-zinc-700 ">No taks to show</h3>
                            <% }%>
        </div>
    </div>
    </body>

</html>